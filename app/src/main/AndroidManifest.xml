<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="smu.it.a2_hw6_1reservationsite21131412116016">

    <!--프로그램이 인터넷에 접근하게 하기 위한 권한 설정-->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- android:usesCleartextTraffic="true" -> 인터넷에서 htts의 보안이 아닌 일반 http 사이트에 접근하기 위하여 설정-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.2조HW61ReservationSite김유진2113141임서현2116016"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReservationPage"/>
        <activity android:name=".ReservationResult"/>
        <activity android:name=".SilenceZoneSelectSeat"/>
        <activity android:name=".NoiseZoneSelectSeat"/>
        <activity android:name=".FloorInfo"/>
        <activity android:name=".IntroduceMonthBook"/>
    </application>

</manifest>